import { createSlice } from "@reduxjs/toolkit"

const initialState = {
  units: []
}

const unitSlice = createSlice({
  name: "unit",
  initialState,
  reducers: {

  }
})

export default unitSlice.reducer