

const FinanceDetailsRow = ({ label, display }) => {

    return (

        <div>
            <div>{label}</div>
            <div>{display}</div>
        </div>

    );
};

export default FinanceDetailsRow;