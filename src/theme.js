import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: "#FF5722",
      light: "#FFCCBC",
      dark: "#E64A19"
    }
  },

})

export default theme