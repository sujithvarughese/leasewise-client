import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: "#1E90FF",
      light: "#FFCCBC",
      dark: "#E64A19"
    }
  },

})

export default theme